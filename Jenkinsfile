pipeline {
    agent { label 'dockerAgent1' }
    stages {
        stage('Fetch git repo and build docker image') {
            steps {
                sh 'ls -la'
            }
        }
    
        }
}
